<%- include('./includes/head.ejs') %>

<% if(isDeadLock){ %>
  <div class="center">
    <h1 class="headings">DEADLOCK</h1>
  </div>
<% } else{ %>
  <% for (var i=0; i < processSequence.length; i++) { %>
    <h1> <%= processSequence[i] %></h1>
    <% } %>
  <% } %>

<%- include('./includes/end.ejs') %>